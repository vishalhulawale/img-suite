services:
  backend:
    build:
      context: ..
      dockerfile: pdf-suite/backend/Dockerfile
    container_name: pdf-suite-backend
    restart: unless-stopped
    expose:
      - "8000"
    volumes:
      - tmp_data:/tmp/pdf_suite_temp
    environment:
      - PYTHONUNBUFFERED=1
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: pdf-suite-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_healthy

volumes:
  tmp_data:
    driver: local
